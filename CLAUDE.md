&#x20;   ## Tool use preferences

&#x20;   - Do not spawn Explore subagents for initial codebase orientation. Read files directly.

&#x20;   - Only spawn subagents when a task is explicitly parallelisable or the codebase is very large (>50 files relevant to

&#x20;    the task).

